This directory contains the extension icons.

Production icons should be provided at sizes: 16x16, 48x48, and 128x128 PNG.

Use `icon.svg` as the source to generate the PNG files:
- Icon from SVG: convert -background none icon.svg -resize 16x16 icon16.png
- Or use a tool like https://tools.picsart.com/image/resize/
