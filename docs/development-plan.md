# 开发计划

本文记录当前代码状态和后续优先级。状态仅表示仓库中的实现进度；平台端到端验证仍需在真实账号和页面上完成。

## 当前状态

| 领域 | 状态 | 备注 |
| --- | --- | --- |
| Chrome Manifest V3 扩展骨架 | 已完成 | Popup、Content Script、Service Worker 和权限配置已接入 |
| Rust/WASM 核心 | 已完成 | 平台识别、数据模型、去重、排序和文件名逻辑 |
| X / Twitter | 已实现，待持续实测 | 支持图片，并过滤已知的不完整视频分片 |
| TikTok | 已实现，待持续实测 | 要求提取到直接 CDN 视频 URL |
| Instagram | 已实现，待持续实测 | 覆盖图片、视频和轮播数据结构 |
| 微博 | 已实现，待持续实测 | 包含页面数据、请求拦截和媒体请求规则 |
| OnlyFans | 已实现，待持续实测 | 仅处理当前账号有权访问的已加载内容 |
| 批量下载队列 | 已完成 | 队列持久化、并发控制、重启后状态核对 |
| 自动化检查 | 已完成 | Rust 测试、格式、Clippy、JS 语法和 Manifest 解析 |
| Chrome 应用商店发布 | 未开始 | 仍需实测、商店素材和隐私披露 |

当前 Rust 测试基线：`23 passed`（2026-08-01）。此数字是代码级测试结果，不代表五个平台已经完成真实页面验收。

## P0：稳定性基线

目标：让五个平台的核心路径具备可重复验证的发布标准。

| 任务 | 优先级 | 完成标准 |
| --- | --- | --- |
| 五平台端到端测试矩阵 | P0 | 每个平台至少覆盖图片、视频、无结果和未登录/无权限场景 |
| 固定脱敏测试样本 | P0 | 为各解析器补充不含个人数据的 HTML/JSON fixture |
| JavaScript 自动化测试 | P0 | 覆盖去重回退、文件名、队列恢复和错误提示 |
| CI 检查全部拦截器 | P0 | `node --check` 覆盖所有 `*_interceptor.js` |
| 发布包内容清理 | P0 | 排除浏览器生成的 `_metadata` 和无关开发文件 |
| 错误诊断信息 | P1 | 能区分未加载内容、权限不足、URL 过期和平台结构变化 |

里程碑：在受支持的 Chrome 版本上完成测试矩阵，发布包可从干净环境加载并通过基本验收。

## P1：下载体验

目标：提升大量媒体下载时的可控性和可恢复性。

| 任务 | 优先级 | 完成标准 |
| --- | --- | --- |
| 设置界面 | P1 | 可配置下载目录、并发数和默认选择行为 |
| 队列详情与失败重试 | P1 | 展示单项状态，并允许只重试失败项 |
| 日期和媒体类型筛选 | P1 | 下载前可按时间、图片或视频筛选 |
| 自动加载更多 | P1 | 明确提示并受用户控制，设置停止条件和最大扫描量 |
| 视频源质量选择 | P1 | 可识别多个完整视频源并优先选择合适清晰度 |
| 国际化 | P2 | 至少提供中文和英文界面 |

## P2：发布与合规

目标：满足 Chrome 应用商店提交要求。

| 任务 | 优先级 | 完成标准 |
| --- | --- | --- |
| 权限审计 | P0 | 逐项验证权限必要性，移除未使用权限和主机范围 |
| 隐私政策与数据披露 | P0 | 文档与实际数据流、权限和存储行为一致 |
| 品牌与商店素材 | P1 | 完成图标、截图、说明和支持页面 |
| 发布前回归 | P0 | 在全新 Chrome 配置中完成安装、扫描、下载和升级验证 |
| 版本与变更日志 | P1 | Manifest、Git 标签、Release 说明保持一致 |

里程碑：商店提交材料齐全，发布候选版本通过权限与功能回归。

## P3：后续方向

以下功能暂不进入发布阻塞范围，实施前需要单独评估复杂度和平台政策：

- 下载前快速预览视频
- 将选中媒体打包为 ZIP
- Edge 和 Firefox 适配
- 深色/浅色主题切换
- 用户主动授权的外部存储导出

## 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 平台 DOM 或内部数据格式变化 | 扫描结果为空或不完整 | 多信号回退、脱敏 fixture、平台级回归测试 |
| 签名 URL 过期 | 预览可见但下载失败 | 缩短扫描到下载的间隔，明确提示重新扫描 |
| 分段视频流 | 下载到不完整文件 | 识别并拒绝分片 URL，只接受可验证的完整媒体源 |
| Service Worker 被回收 | 下载进度丢失 | 先持久化队列，并在启动时核对 Chrome 下载状态 |
| 过宽权限影响审核与信任 | 商店审核失败或用户拒绝安装 | 发布前执行权限审计，说明每项权限用途 |
| 平台条款或内容权利问题 | 合规风险 | 只处理当前账号可访问内容，不实现权限绕过，并提供清晰使用说明 |

## 发布流程

1. 更新 `extension/manifest.json` 的版本号。
2. 运行本地质量检查和五平台回归测试。
3. 创建与 Manifest 一致的标签，例如 `v1.0.0`。
4. 推送标签，触发 `.github/workflows/release.yml`。
5. 验证 Release 中 ZIP 的内容和 SHA-256 校验文件。
6. 从发布 ZIP 解压并完成一次干净安装验收。

## 版本记录

| 版本 | 状态 | 范围 |
| --- | --- | --- |
| `1.0.0` | 开发中 | 五平台解析、WASM 处理、持久化批量下载队列 |
